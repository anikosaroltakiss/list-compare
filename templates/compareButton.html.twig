<button 
    class="mt-6 w-full bg-indigo-600 text-white py-3 rounded-md font-bold hover:bg-indigo-700 transition"
    hx-post="/compare"
    hx-target="#results-section"
    hx-include="{{ include_selectors }}"
    hx-vals='{"type": "{{ type_id }}"}'
>
	Összehasonlítás
</button>
